/**
 * Context collapse persistence — saves/restores collapse state.
 * Feature-gated: CONTEXT_COLLAPSE
 */

export function restoreFromEntries(
  _commits: unknown[],
  _snapshot: unknown,
): void {
  // Restore collapse state from persisted entries
}
