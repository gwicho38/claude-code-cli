/**
 * Local skill search — indexes and queries local skill files.
 *
 * Translated from:
 *   - Python: claw-code/src/execution_registry.py (score-based routing)
 */

export function clearSkillIndexCache(): void {
  // Clears the in-memory skill index so it's rebuilt on next search
}
