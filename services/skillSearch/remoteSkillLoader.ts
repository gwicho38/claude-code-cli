/**
 * Remote skill loader — fetches and caches skills from remote registries.
 */

export async function loadRemoteSkill(
  _name: string,
): Promise<{ content: string; metadata: Record<string, unknown> } | null> {
  return null
}
