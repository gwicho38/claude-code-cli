/**
 * Skill search telemetry — logs remote skill loading events.
 */

export function logRemoteSkillLoaded(
  _name: string,
  _metadata?: Record<string, unknown>,
): void {
  // Telemetry event for skill load tracking
}
