/**
 * MCP skill integration — fetches skill definitions from MCP servers.
 * Feature-gated: MCP_SKILLS
 */

export async function fetchMcpSkillsForClient(
  _clientName: string,
): Promise<unknown[]> {
  return []
}
